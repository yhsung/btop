# M3 Draw Port (C++ golden harness + btop-draw) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port `btop_draw.cpp` (2551 lines) to a new `btop-draw` crate at byte-identical ANSI output, proven by a C++ golden harness whose `.ans` fixtures Rust tests compare against.

**Architecture:** `tests/draw_golden.cpp` links `libbtop` (OBJECT lib, no gtest needed) and dumps box outputs for fixed synthetic scenarios to `src-rust/fixtures/draw/*.ans` (volatile spans normalized). `btop-draw` transcribes Meter/Graph/boxes per-box; each box task ends with byte-compare tests green. Zero third-party deps (wcwidth table converted by script, committed as data).

**Tech Stack:** Rust 2021, `cargo test`, C++ harness via CMake `BUILD_TESTING=ON`, python3 conversion script (build-time only, output committed).

---

## Scope note

M3 covers draw only: scaffold (Task 2), boxes + calcSizes (Task 3), cpu (Task 4), mem+net (Task 5), gpu (Task 6), proc + gates (Task 7). TextEdit/menu/app/runner integration are M4. C++ truth: `src/btop_draw.cpp` line refs below; read-only, never modify (except ADDITIVE `tests/` files + one CMake target).

## File structure

- Create: `tests/draw_golden.cpp`, `src-rust/fixtures/draw/*.ans`, `src-rust/tools/convert_wcwidth.py` (build-time, output committed).
- Modify: `tests/CMakeLists.txt` (append `draw_golden` target — read the file first, 20 lines).
- Create: `src-rust/btop-draw/Cargo.toml` (deps: `btop-tools`, `btop-config` by path), `src/lib.rs`, `src/ansi.rs`, `src/symbols.rs`, `src/meter_graph.rs`, `src/boxes.rs`, `src/theme_grad.rs`, `src/wcwidth.rs` (generated), `src/cpu.rs`, `src/mem.rs`, `src/net.rs`, `src/gpu.rs`, `src/proc_.rs`.
- Modify: `src-rust/Cargo.toml` (add member), `src-rust/btop-tools/src/strtools.rs` (append floating_humanizer + wide-mode fns).
- Create: `.github/workflows/draw-golden.yml` (drift job, SHA-pinned per repo convention).
- Golden-test pattern (same as Plan 1 `golden.rs`): `btop-draw/tests/golden.rs` reads `../fixtures/draw/*.ans` via `CARGO_MANIFEST_DIR`, asserts byte equality with port output for identical synthetic inputs.

## Scenario catalog (SINGLE source of truth — transcribe to BOTH harness and Rust tests; reviewer cross-checks)

- `S0 width100`: Term 100x30, theme Default, tty off, lowcolor off. All boxes use this unless noted.
- `S1 width160`: Term 160x48 (wide layout branch).
- `S2 tty`: tty graph symbols + ASCII box chars (force_tty path).
- Synthetic cpu: 4 cores, total deque [..20 values..] — EXACT values chosen by Task 1 implementer (must be fixed literals, e.g. ascending 10..80), temps [45..50], load_avg [1.5,1.2,1.0]. Task 1 DEFINES the literals; later tasks reuse them verbatim.
- Volatile normalization (capture step): clock `HH:MM:SS` → `<CLOCK>`, uptime `..d..:..` → `<UPTIME>`, dates → `<DATE>`. Task 1 proves determinism: run harness twice, diff empty after normalization.

---

### Task 1: C++ golden harness + fixtures

**Files:** Create `tests/draw_golden.cpp`; Modify `tests/CMakeLists.txt`; Create `src-rust/fixtures/draw/` outputs.

- [ ] **Step 1: Read the build + init seams.** Read `tests/CMakeLists.txt` (20 lines), root `CMakeLists.txt:47-80` (libbtop OBJECT), `src/btop.cpp:272-330` (Term::init/Config::load), `:1044-1045` (Theme init). Find `Term::width/height` declaration (likely `btop_tools.hpp` Term namespace) — harness sets them DIRECTLY (never call `Term::init()` headless; document why).

- [ ] **Step 2: Write the harness skeleton (must compile before scenarios).**
```cpp
// tests/draw_golden.cpp — links libbtop, dumps synthetic box outputs.
#include <cstdio>
#include <string>
#include "btop_config.hpp"
#include "btop_theme.hpp"
#include "btop_shared.hpp"
#include "btop_draw.hpp"
#include "btop_tools.hpp"

static void emit(const std::string& name, const std::string& out) {
    printf("@@BEGIN:%s@@\n%s\n@@END:%s@@\n", name.c_str(), out.c_str(), name.c_str());
}
int main() {
    // init WITHOUT terminal: Config defaults + Theme Default + fixed Term size.
    // (mirror btop.cpp:328,1044-1045; set Term::width/height directly.)
    // scenarios appended in Steps 3-4.
    return 0;
}
```
CMake (append to tests/CMakeLists.txt):
```cmake
add_executable(draw_golden draw_golden.cpp)
target_link_libraries(draw_golden libbtop)
```
Build: `cmake -B build -DBUILD_TESTING=ON && cmake --build build --target draw_golden`. Expected: links clean (no gtest needed for this target).

- [ ] **Step 3: Add Meter/Graph/symbols scenarios.** Fixed inputs: Meter(50,"cpu",false)(75); Graph width 20 height 5 with data {10,20,...,80}; tty variants (S2). Emit blocks `meter_50`, `graph_default`, `graph_tty`. Define the EXACT literals here — later tasks transcribe them.

- [ ] **Step 4: Add cpu/mem/net/gpu/proc box scenarios (S0+S1 widths).** Populate shared structs with fixed literals (DEFINE the scenario catalog values here — e.g. cpu total deque, temps, load_avg; keep each deque ≤20 values, fixed). Call each `X::draw(info, true /*force_redraw*/, false)`. Emit `cpu_S0`, `mem_S0`, … Run twice, prove determinism after normalization; write the normalization into a capture script `src-rust/fixtures/draw/capture.sh`:
```bash
#!/bin/sh
# usage: ./capture.sh <draw_golden_bin> <outdir>
"$1" | python3 -c "import sys,re; t=sys.stdin.read();
t=re.sub(r'\d{2}:\d{2}:\d{2}','<CLOCK>',t);
t=re.sub(r'\d+d \d{2}:\d{2}(:\d{2})?','<UPTIME>',t);
open(sys.argv[1],'w').write(t)" /dev/stdin
```
(Split per-@@BEGIN@@ block into `<name>.ans` — implement in the python one-liner or small script; exact mechanics your choice, reviewer checks outputs.)

- [ ] **Step 5: Commit.** `git add tests/draw_golden.cpp tests/CMakeLists.txt src-rust/fixtures/draw/ && git commit -m "test(cpp): add draw golden harness with S0/S1/S2 fixtures"`.

### Task 2: Scaffold — tools gaps, wcwidth, ansi, Meter/Graph

**Files:** Modify `btop-tools/src/strtools.rs`; Create `tools/convert_wcwidth.py`, `btop-draw/{Cargo.toml,src/*.rs}` scaffold files.

- [ ] **Step 1: Port the missing tools (TDD, C++ vectors first).** Append to strtools.rs with tests derived from reading `btop_tools.cpp` (find each fn, transcribe formula + hand-computed vectors):
  - `floating_humanizer(value, shorten, start, bit, per_second)` — read full body (unit tables Byte/KiB… + bit variants + per_second suffix); note it reads `Config::getB("base_10_sizes")` — Rust signature takes an explicit `base_10: bool` param instead (no globals; document).
  - `wide_ulen(str)` + `uresize(str,len,wide=true)` + `luresize` wide path + `cjust` (mirror ljust/rjust pattern from Plan 1) + `trans` (space→Mv::r run; needs Mv — implement `mv_r(n)` in btop-draw ansi.rs and have trans take it as a closure? NO — simpler: port trans INTO btop-draw operating on strings with explicit `"\\x1b[{n}C"`-producing helper. Document the placement.)
- [ ] **Step 2: Convert the wcwidth table.** Read `include/widechar_width.hpp` (1570 lines — data tables + lookup fn). Write `src-rust/tools/convert_wcwidth.py` that parses the C++ interval tables and emits `src-rust/btop-draw/src/wcwidth.rs` as `const RANGES: &[(u32,u32)]` + `pub fn wcwidth(ch: char) -> usize` (binary search, same semantics incl. zero-width/control handling — verify against the C++ fn, not just tables). Commit BOTH script and output. Tests: ASCII→1, CJK→2, combining→0, emoji spot checks (3-4 literals verified against C++ behavior by reading the table entries).
- [ ] **Step 3: ansi.rs + symbols.rs + meter_graph.rs.** Port `Mv::{to,u,d,l,r}` + `Fx::{b,ub,reset,ul,...}` (find defs — likely btop_tools.hpp Mv/Fx namespaces; exact ESC strings), `Symbols` tables (btop_draw.cpp:84-135 incl. 25-entry braille + tty variants + graph_symbol map), `Meter` (398-419) + `Graph` (422-541) with gradient input as `&[String; 101]` slices (built by theme_grad, Task 3? NO — gradients needed NOW for Meter/Graph tests: build minimal gradient construction in this task, full theme integration in Task 3).
  Byte-compare tests vs Task 1 `meter_50`, `graph_default`, `graph_tty` fixtures. Commit: `feat(rust): btop-draw scaffold (ansi, symbols, meter, graph)`.

### Task 3: Boxes, banner, clock, calcSizes, theme gradients

**Files:** Modify `btop-draw` (+`boxes.rs`, `theme_grad.rs`), fixtures as needed.
- Port `createBox` (279-331, tty/rounded branches), `banner_gen` (138-177), `update_clock` (333-395 — wall-time: Rust takes explicit `now: &str` params? NO — mirror C++ shape but pass a `Clock { time: String, date: String }` struct built by caller; harness normalizes anyway; document), `calcSizes` (2248-2551 — layout math over (term_w/h, shown_boxes); pure fn taking explicit inputs, no globals).
- `theme_grad.rs`: port gradient construction from `btop_theme.cpp` (READ it: find generateColors/gradient fn, 101-entry arrays, 256-mode branch) over `btop-config` hex/color fns.
- Golden tests vs new harness blocks (`box_*`, `banner_*`; clock covered via normalization). Commit.

### Task 4: cpu box

Transcribe `Cpu::draw` (567-1029) to `cpu.rs` as pure `draw_cpu(input: &CpuDrawInput, theme, config-flags-struct, width) -> String` where input structs mirror the EXACT shared fields used (cpu_percent map, core_percent, temp, load_avg, watts + the globals the fn reads — bundle as explicit params, no globals). Byte-compare vs `cpu_S0`, `cpu_S1` (+tty variant if harness emitted it). Commit.

### Task 5: mem + net boxes

Same pattern for `Mem::draw` (1237-1487), `Net::draw` (1499-1602). Note mem disks unify with `disk_usage` (already in btop-collect — reuse? draw needs used/total/percent only; reuse btop-collect fns where exact, else transcribe; document each reuse). Net speeds reuse `floating_humanizer` (Task 2). Byte-compare, commit.

### Task 6: gpu box

`Gpu::draw` (1050-1221). Same pattern. Commit.

### Task 7: proc box + gates

`Proc::draw` (1707-2246 — hardest: tree/sort/filter/selection/detail/mouse maps; mouse mappings recorded into an explicit `&mut Vec<MouseMap>` param instead of Input global; TextEdit filter is M4 — stub the filter row with current behavior noted? NO stubs: transcribe fully, filter state as explicit param struct).
Then: full gate (`cargo test/clippy/fmt`), new `.github/workflows/draw-golden.yml` drift job (rebuild harness on `src/**`+`tests/**` changes, re-capture, diff fixtures — SHA-pin actions per repo convention, self-trigger paths), commit both.

## Self-review

- Spec coverage: harness (T1) → scaffold+tools gaps (T2) → boxes/layout/gradients (T3) → cpu (T4) → mem/net (T5) → gpu (T6) → proc+gates (T7). calcSizes in T3 (layout before boxes — correct order since boxes need geometry). floating_humanizer/wide-mode/cjust/trans/wcwidth gaps from Plan 1 explicitly closed in T2 (final-review ledger item).
- Placeholders: transcription tasks name EXACT function line ranges instead of inline code (2500 lines cannot be inlined — deliberate, documented adaptation of the writing-plans rule; each step still has exact file paths, commands, and byte-equality acceptance). Scenario literals are Task 1-defined (single source of truth) — later tasks reference, never redefine.
- Type consistency: `VecDeque<i64>` histories, `&[String; 101]` gradients, `u64` bytes, explicit input structs per box (no globals), `proc_` naming, `Result` never used for draw (String-only, per spec S2 precedent).
- Risks flagged, not hidden: Term::init headless (set width/height directly); FetchContent network at configure (CI + local have it; draw_golden itself needs no gtest); C++ harness init-sequence guesswork (verify-and-adjust against btop.cpp:272-330,1044-1045); proc tree complexity (dedicated task); volatile normalization list may grow (Task 1 proves determinism by double-run diff).
